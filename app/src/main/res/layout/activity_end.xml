<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".EndActivity">

    <TextView
        android:id="@+id/final_message"
        style="@style/ThankYouMessageStyle"
        android:layout_above="@id/sardinia_flag_icon"
        android:text="@string/final_message_text"
        app:fontFamily="serif" />

    <ImageView
        android:id="@+id/sardinia_flag_icon"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_centerInParent="true"
        app:srcCompat="@drawable/sardinian_head" />

    <TextView
        android:id="@+id/thank_you_text"
        android:text="@string/thank_you"
        style="@style/ThankYouMessageStyle"
        android:layout_below="@id/sardinia_flag_icon"
        app:fontFamily="serif" />

</RelativeLayout>